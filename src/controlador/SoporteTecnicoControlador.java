package controlador;

import modelo.Nivel;
import modelo.SupertlonSingleton;
import modelo.Usuario;

public class SoporteTecnicoControlador {

	/*
	public void crearSede(String barrio, Nivel nivel, double precioAlquiler) {
		SupertlonSingleton supertlonSingleton = SupertlonSingleton.getInstance();
		Usuario soporteTecnico = supertlonSingleton.getUsuarioActual();
		soporteTecnico.crearSede(barrio, nivel, precioAlquiler);

	};
	*/

}
